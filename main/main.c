#include <stdio.h>
#include <string.h>

#include "freertos/FreeRTOS.h"
#include "freertos/task.h"

#include "esp_ds.h"
#include "esp_efuse.h"
#include "esp_random.h"

#include "mbedtls/platform.h"
#include "mbedtls/entropy.h"
#include "mbedtls/ctr_drbg.h"
#include "mbedtls/bignum.h"
#include "mbedtls/rsa.h"

#define ATTEST_KEY_SLOT     (4)
//#define BLOCK      (EFUSE_BLK_KEY4)
//#define KEY        (HMAC_KEY4)

uint32_t ticks() {
    return xTaskGetTickCount();
}

void delay(uint32_t duration) {
    vTaskDelay((duration + portTICK_PERIOD_MS - 1) / portTICK_PERIOD_MS);
}

void panic(char *message, int code) {
    printf("[PANIC] %s (code=%d; %s)\n\n", message, code,
      esp_err_to_name(code));
    while(1) { delay(1000); }
}

void reverseBytes(uint8_t *data, size_t length) {
    for (int i = 0; i < length / 2; i++) {
        uint8_t tmp = data[i];
        data[i] = data[length - 1 - i];
        data[length - 1 - i] = tmp;
    }
}

void reverseWords(uint8_t *data, size_t words) {
    for (int i = 0; i < words; i += 4) {

        uint8_t tmp = data[i + 0];
        data[i + 0] = data[i + 3];
        data[i + 3] = tmp;

        tmp = data[i + 1];
        data[i + 1] = data[i + 2];
        data[i + 2] = tmp;
    }
}

void copy_value(uint8_t *dst, uint8_t *src, size_t length) {
    for (int i = 0; i < length; i++) { dst[i] = src[i]; }

    //if (reverseBytes) {
    for (int i = 0; i < length / 2; i++) {
        uint8_t tmp = dst[i];
        dst[i] = dst[length - 1 - i];
        dst[length - 1 - i] = tmp;
    }
    //}
    /*
    if (reverseWords) {
        for (int i = 0; i < length; i += 4) {
            uint8_t tmp = dst[i + 0];
            dst[i + 0] = dst[i + 3];
            dst[i + 3] = tmp;

            tmp = dst[i + 1];
            dst[i + 1] = dst[i + 2];
            dst[i + 2] = tmp;
        }
    }
    */
}

static int copy_mpi(uint32_t keySize, mbedtls_mpi* value, uint32_t *dst) {
    uint8_t tmp[keySize / 8];
    int ret = mbedtls_mpi_write_binary(value, tmp, keySize / 8);
    if (ret) { panic("failed to write_binary", ret); }
    copy_value((uint8_t*)dst, tmp, keySize / 8);
    return ret;
}

//https://github.com/espressif/esp_secure_cert_mgr/issues/8
static void compute_rinv_mprime(uint32_t keySize, mbedtls_mpi* N, mbedtls_mpi* rinv, uint32_t* mprime) {
    // python equivalent code:
    //
    //    key_size = private_key.key_size # in bits
    //
    //    # calculate rinv == Rb
    //    rr = 1 << (key_size * 2)
    //    rinv = rr % pub_numbers.n # RSA r inverse operand
    //
    //    # calculate MPrime
    //    a = rsa._modinv(M, 1 << 32)
    //    mprime = (a * -1) & 0xFFFFFFFF # RSA M prime operand

    // rr = 1 << (key_size * 2) # in bits
    mbedtls_mpi rr;
    mbedtls_mpi_init(&rr);
    mbedtls_mpi_lset(&rr, 1);
//    mbedtls_mpi_shift_l(&rr, sizeof(pd_4096_bit_t) * 8 * 2);
    mbedtls_mpi_shift_l(&rr, keySize * 2); // RicMoo!

    // rinv = rr % rsa.N
    mbedtls_mpi_mod_mpi(rinv, &rr, N);

    // ls32 = 1 << 32
    mbedtls_mpi ls32;
    mbedtls_mpi_init(&ls32);
    mbedtls_mpi_lset(&ls32, 1);
    mbedtls_mpi_shift_l(&ls32, 32);

    // a = rsa._modinv(N, 1 << 32)
    mbedtls_mpi a;
    mbedtls_mpi_init(&a);
    mbedtls_mpi_inv_mod(&a, N, &ls32);

    // a32 = a
    uint32_t a32 = 0;
    mbedtls_mpi_write_binary_le(&a, (uint8_t*) &a32, sizeof(uint32_t));

    // mprime
    *mprime = ((int32_t) a32 * -1) & 0xFFFFFFFF;
}

static int getKeyBlock(int slot) {
    switch(slot) {
        case 0: return EFUSE_BLK_KEY0;
        case 1: return EFUSE_BLK_KEY1;
        case 2: return EFUSE_BLK_KEY2;
        case 3: return EFUSE_BLK_KEY3;
        case 4: return EFUSE_BLK_KEY4;
    }
    panic("unknown slot block", slot);
    return -1;
}

static int getHmacKey(int slot) {
    switch(slot) {
        case 0: return HMAC_KEY0;
        case 1: return HMAC_KEY1;
        case 2: return HMAC_KEY2;
        case 3: return HMAC_KEY3;
        case 4: return HMAC_KEY4;
    }
    panic("unknown hmac slot", slot);
    return -1;
}

#define KEY_SIZE   (ESP_DS_SIGNATURE_MAX_BIT_LEN)


#define EXPONENT 65537


void dumpMpi(char *header, mbedtls_mpi* value) {
    char str[2048];
    size_t olen = 0;

    int ret = mbedtls_mpi_write_string(value, 16, str, sizeof(str), &olen);

    if (ret == 0) {
        printf("%s%s (length=%d bits)\n", header, str, (olen - 1) * 4);
    } else {
        printf("%s[FAILED]\n", header);
    }
}

void dumpBuffer(char *header, uint8_t *buffer, size_t length) {
    static char *hex = "0123456789abcdef";

    printf("%s", header);
    for (uint32_t i = 0; i < length; i++) {
        printf("%c%c", hex[buffer[i] >> 4], hex[buffer[i] & 0x0f]);
    }
    printf("  (%d bytes)\n", length);
}

void dumpArray(char *header, uint8_t *buffer, size_t length) {
    static char *hex = "0123456789abcdef";

    printf("%s = {", header);
    for (uint32_t i = 0; i < length; i++) {
        printf("0x%c%c, ", hex[buffer[i] >> 4], hex[buffer[i] & 0x0f]);
    }
    printf("  }\n");
}


typedef struct KeyPair {
    mbedtls_mpi N, E, P, Q, D, Rb;
    uint32_t key_size;
    uint32_t m_prime;
} KeyPair;


static int get_params(KeyPair *keypair, esp_ds_p_data_t *params) {
    int ret = 0;

    params->length = (keypair->key_size / 32) - 1;

    ret = copy_mpi(keypair->key_size, &keypair->N, params->M);
    if (ret) { return ret; }
    dumpBuffer("<PARAMS-M=", (uint8_t*)params->M, sizeof(params->M));

    ret = copy_mpi(keypair->key_size, &keypair->D, params->Y);
    if (ret) { return ret; }
    dumpBuffer("!<PARAMS-Y=", (uint8_t*)params->Y, sizeof(params->Y));

    ret = copy_mpi(keypair->key_size, &keypair->Rb, params->Rb);
    if (ret) { return ret; }

    params->M_prime = keypair->m_prime;

    return ret;
}

// https://github.com/Mbed-TLS/mbedtls/blob/development/programs/pkey/rsa_genkey.c
static int generate_keypair(KeyPair *keypair, uint32_t key_size) {
    keypair->key_size = key_size;

    int ret = 1;

    mbedtls_rsa_context rsa;
    mbedtls_entropy_context entropy;
    mbedtls_ctr_drbg_context ctr_drbg;

    /**
      Public:
        N - the RSA modulus
        E - the public exponent
      Private:
        P - the first prime factor of N
        Q - the second prime factor of N
        D - the private exponent
     */
    const char *pers = "rsa_genkey";

    mbedtls_ctr_drbg_init(&ctr_drbg);
    mbedtls_rsa_init(&rsa);
    mbedtls_mpi_init(&keypair->N);
    mbedtls_mpi_init(&keypair->P);
    mbedtls_mpi_init(&keypair->Q);
    mbedtls_mpi_init(&keypair->D);
    mbedtls_mpi_init(&keypair->E);
    mbedtls_mpi_init(&keypair->Rb);

    uint32_t m_prime = 0;


    printf("[INFO] Seeding random number generator\n");

    mbedtls_entropy_init(&entropy);
    if ((ret = mbedtls_ctr_drbg_seed(&ctr_drbg, mbedtls_entropy_func,
      &entropy, (const unsigned char *) pers, strlen(pers))) != 0) {
        printf("[ERROR] mbedtls_ctr_drbg_seed=%d\n", ret);
        return -1;
    }


    printf("[INFO] Generating the RSA key (%ld-bit)\n", key_size);

    if ((ret = mbedtls_rsa_gen_key(&rsa, mbedtls_ctr_drbg_random,
      &ctr_drbg, key_size, EXPONENT)) != 0) {
        printf("[ERROR] mbedtls_rsa_gen_key=%d\n", ret);
        return -2;
    }


    printf("[INFO] Exporting the public key\n");

    if (mbedtls_rsa_export(&rsa, &keypair->N, &keypair->P,
      &keypair->Q, &keypair->D, &keypair->E)) {
        printf("[ERROR] could not export RSA parameters\n");
        return -3;
    }

    dumpMpi("<RSA-PUBKEY-N=", &keypair->N);
    dumpMpi("<RSA-PUBKEY-E=", &keypair->E);
    dumpMpi("!<RSA-PUBKEY-P=", &keypair->P);
    dumpMpi("!<RSA-PUBKEY-Q=", &keypair->Q);
    dumpMpi("!<RSA-PUBKEY-D=", &keypair->D);


    compute_rinv_mprime(key_size, &keypair->N, &keypair->Rb, &m_prime);
    dumpMpi("!<RSA-PUBKEY-rInv=", &keypair->Rb);

    keypair->m_prime = m_prime;
    printf("!<M_prime=%lx\n", m_prime);

    mbedtls_rsa_free(&rsa);
    mbedtls_ctr_drbg_free(&ctr_drbg);
    mbedtls_entropy_free(&entropy);

    return 0;
}


void dumpKey(int slot) {
    int block = getKeyBlock(slot);

    bool unused = esp_efuse_key_block_unused(block);
    bool readProtect = esp_efuse_get_key_dis_read(block);
    bool writeProtect = esp_efuse_get_key_dis_write(block);
    printf("[INFO] BLK=%d, unused=%d, readProtect=%d, writeProtect=%d\n", block, unused, readProtect, writeProtect);

    uint8_t key[32];
    esp_efuse_read_block(block, key, 0, sizeof(key) * 8);
    dumpBuffer("KEY=", key, sizeof(key));
}

void app_main() {
    int ret = 0;
    printf("[INFO] start provisioning\n");
    printf("SIZE: rsa_size=%d messageLength=%d\n", ESP_DS_RSA_3072,
      (ESP_DS_RSA_3072 + 1) * 4);


    esp_ds_data_t *cipherData = heap_caps_malloc(sizeof(esp_ds_data_t),
      MALLOC_CAP_DMA);
    memset(cipherData, 0, sizeof(esp_ds_data_t));

    /*
    // Create an RSA keypair
    KeyPair keypair;
    ret = generate_keypair(&keypair, KEY_SIZE);
    if (ret) { panic("failed to generate RSA key", ret); }

    // Convert it to the ESP format
    esp_ds_p_data_t params = { 0 };
    ret = get_params(&keypair, &params);
    if (ret) { panic("failed to generate RSA key", ret); }
    dumpBuffer("!<params=", (uint8_t*)&params, sizeof(esp_ds_p_data_t));

    // Encrypt it for use by the ESP DS hardware
    uint8_t iv[16] = { 0 };
    esp_fill_random(iv, sizeof(iv));
    dumpBuffer("!<IV=", iv, sizeof(iv));

    uint8_t key[32] = { 0 };
    esp_fill_random(key, sizeof(key));
    dumpBuffer("!<KEY=", key, sizeof(key));

    //esp_ds_data_t *cipherData = heap_caps_malloc(sizeof(esp_ds_data_t),
    //  MALLOC_CAP_DMA);
    //memset(cipherData, 0, sizeof(esp_ds_data_t));

    ret = esp_ds_encrypt_params(cipherData, iv, &params, key);
    if (ret) { panic("failed to encrypt params", ret); }
    dumpBuffer("<cipherData=", (uint8_t*)cipherData, sizeof(esp_ds_data_t));
    dumpArray("<cipherData=", (uint8_t*)cipherData, sizeof(esp_ds_data_t));

    // Burn it to the eFuse
    ret = esp_efuse_write_key(getKeyBlock(ATTEST_KEY_SLOT),
      ESP_EFUSE_KEY_PURPOSE_HMAC_DOWN_DIGITAL_SIGNATURE, key, 32);
    if (ret) { panic("failed to write key", ret); }
    printf("[INFO] write=%d\n", ret);
    */

    dumpKey(ATTEST_KEY_SLOT);


    {
        uint8_t value[] = {  };
        memcpy((uint8_t*)cipherData, value, sizeof(esp_ds_data_t));
    }
    dumpBuffer("cipherData=", (uint8_t*)cipherData, sizeof(esp_ds_data_t));


    uint8_t *messageHash = heap_caps_malloc(384, MALLOC_CAP_DMA);
    uint8_t *signature = heap_caps_malloc(384, MALLOC_CAP_DMA);

    memset(messageHash, 0x0, 384);
    memset(signature, 0x0, 384);
    messageHash[0] = 0x01;
    dumpBuffer("messageHash=", messageHash, 384);

    ret = esp_ds_sign(messageHash, cipherData, getHmacKey(ATTEST_KEY_SLOT), signature);
    printf("Sig: ret=%d %s ", ret, esp_err_to_name(ret));
    dumpBuffer("sig=", signature, 384);


    // Begin accepting input from the provisioning service
    printf("[INFO] listening for commands\n");
    printf("<READY\n");

    uint8_t buffer[512];

    size_t offset = 0;
    buffer[0] = 0;

    while (1) {
        // Not enough space left in the buffer; purge it
        if (sizeof(buffer) - offset - 1 < 1) {
            printf("[WARNING] buffer exceeded length, purging\n");

            // Purge the input buffer
            for (uint32_t i = 0; i < 128; i++) {
                fread(buffer, 1, sizeof(buffer) - 1, stdin);
                delay(1);
            }

            offset = 0;
            buffer[0] = 0;
        }

        // Read anything in the stdin buffer
        //size_t length = fread(&buffer[offset], 1, sizeof(buffer) - offset - 1, stdin);
        size_t length = fread(&buffer[offset], 1, sizeof(buffer) - offset - 1, stdin);
        offset += length;
        buffer[offset] = 0;

        for (uint32_t i = 0; i < offset; i++) {

            // Null termination
            if (buffer[i] == 0) { break; }

            // New line; we have a new command
            if (buffer[i] == 10) {
                buffer[i] = 0;

                printf("EXEC(%ld): %s\n", i, buffer);
                //if (buffer[0] == 'd') {
                //    dumpKey(getKeyBlock(k));
                //}

                offset = 0;
                buffer[0] = 0;

                break;
            }
        }

        delay(1);
    }

    //heap_caps_free(cipherData);

    while (1) {
        printf("<DONE\n");
        delay(10000);
    }
}
